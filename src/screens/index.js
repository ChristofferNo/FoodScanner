// A centralized entry point for all screens, simplifying imports.

import HomePage from "./HomePage";

const Screens = { // central export makes it easier to import multiple Screens from other parts of your application using a single import statement.
    HomePage
};

export default Screens